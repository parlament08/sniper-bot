[pytest]
python_files = test_*.py
testpaths = tests
markers =
    integration: deterministic integration tests that do not require external services
    manual: manual diagnostics, not part of the automated test suite
